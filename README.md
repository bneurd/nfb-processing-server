# nfb-processing-server

**Install the liblsl library when using linux**

https://github.com/sccn/liblsl

For conda users: 

``conda install -c conda-forge liblsl``

Then install the pip requirements:

`pip install -r requirements.txt`